{
  "scripts": {
    "sync-version": "node sync-version.js",
    "predeploy": "npm run sync-version",
    "watch": "pm2 start ecosystem.config.js",
    "stop": "pm2 stop config-watcher",
    "status": "pm2 status"
  },
  "devDependencies": {
    "pm2": "^5.3.0"
  }
}
